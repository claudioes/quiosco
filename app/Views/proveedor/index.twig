{% extends "template/base.twig" %}

{% block jsapp %}
    <script src="{{ siteUrlWithVersion('js/app/proveedor.js') }}"></script>
    <script>
        Proveedor.IndexView();
    </script>
{% endblock %}

{% block content %}
    <h1>Proveedores</h1>

    <div id="toolbar" class="btn-group" role="group">
        <a href="{{ urlFor('Proveedor:create') }}" class="btn btn-success">
            <i class="glyphicon glyphicon-plus"></i> Nuevo
        </a>
    </div>

    <div class="table-responsive">
        <table id="table" class="table table-bordered table-hover">
            <thead>
                <tr>
                    <th data-data="codigo" data-class-name="text-right">Código</th>
                    <th data-data="nombre" style="width:100%">Nombre</th>
                    <th data-data="accion" data-sortable="false" data-searchable="false"></th>
                </tr>
            </thead>
        </table>
    </div>
{% endblock %}
